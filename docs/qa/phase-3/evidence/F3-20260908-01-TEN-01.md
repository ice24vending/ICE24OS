# Evidencia F3-20260908-01 — TEN-01

## Datos comunes

- `RUN_ID`: `F3-20260908-01`
- `ID_CASO`: `TEN-01`
- `NOMBRE`: Propietarios exclusivos en UI
- `REQUISITO`: F3-05/F3-06 — aislamiento de cuentas y denegación por defecto
- `ENTORNO`: LOCAL; `http://localhost:3000`; Supabase local
- `COMMIT_Y_CAMBIOS`: base `11148e9dbadbc160ee4fe1d5d33ec714308905a7` con cambios locales; incluye corrección pendiente de commit en `apps/api/src/modules/identity/identity.store.ts`
- `CONFIGURACION`: API `127.0.0.1:3001`; Supabase `127.0.0.1:54321`; OIDC RS256; secretos sólo por alias
- `EJECUTOR`: usuario solicitante; nombre nominal pendiente de registrar
- `INICIO_FIN`: 2026-09-08; horas y zona exactas pendientes de registrar
- `CLIENTE`: Windows y Brave; versión exacta pendiente de registrar; perfiles de navegador independientes confirmados
- `REFERENCIAS_SECRETOS`: credenciales sintéticas OW-A/OW-B; valores no registrados
- `CORRELACION_AUDITORIA`: pendiente de anexar antes del cierre
- `DEFECTO_Y_SEVERIDAD`: ninguno observado
- `LIMPIEZA`: las cuentas y membresías sintéticas permanecen activas para TEN-02 a TEN-06
- `REEJECUCION`: no aplica
- `APROBADOR`: solicitante confirmó el resultado en la sesión Codex; nombre/firma nominal pendiente

## Variante IA → Cuenta A — intento 1 (suplementaria)

- `PREPARACION_DATOS`: `qa-admin@example.test`, rol `IA`, `QA ICE24 Control` activa y perfil de navegador N1
- `PASOS_EJECUTADOS`: inicio de sesión de IA; carga del selector; activación de A; revisión visual de contexto y perfil; comprobación de ausencia de B
- `RESULTADO_ESPERADO`: N1 ofrece únicamente A; HTML y UI no revelan B; no se comparten cookies ni tokens
- `RESULTADO_OBSERVADO`: el ejecutor confirmó que la sesión y el contexto A funcionaron conforme al plan y no revelaron información de B
- `EVIDENCIA_SANITIZADA`: confirmación escrita del ejecutor en la sesión Codex del RUN; captura final sanitizada pendiente de incorporar al expediente restringido
- `ESTADO`: `APROBADO`

Esta variante aporta evidencia de aislamiento en UI, pero no sustituye OW-A/A exigida por el
prerrequisito literal de TEN-01.

## Variante OW-B → Cuenta B — intento 1

- `PREPARACION_DATOS`: `qa-owner-b@example.test`; UID Supabase enlazado; membresía exclusiva activa en `QA Cuenta B RUN`; rol `OW`; perfil de navegador N2
- `PASOS_EJECUTADOS`: inicio de sesión de OW-B; carga del selector; activación de B; revisión visual de contexto y perfil; comprobación de ausencia de A
- `RESULTADO_ESPERADO`: N2 ofrece únicamente B; HTML y UI no revelan A; no se comparten cookies ni tokens
- `RESULTADO_OBSERVADO`: el ejecutor confirmó que la sesión y el contexto B funcionaron conforme al plan y no revelaron información de A
- `EVIDENCIA_SANITIZADA`: confirmación escrita del ejecutor en la sesión Codex del RUN; captura final sanitizada pendiente de incorporar al expediente restringido
- `ESTADO`: `APROBADO`

## Alcance del resultado

Las dos ejecuciones acreditan el aislamiento observado en la UI para IA/A y OW-B/B. TEN-01 queda
`EN_CURSO` hasta preparar y ejecutar OW-A/A. No acreditan activación cruzada por API, modificación
de asociaciones, manipulación de cabeceras, RLS ni ausencia de datos en respuestas capturadas; esos
controles permanecen en `TEN-02` a `TEN-05` y `TEN-15`.

## Variante OW-A → Cuenta A — pendiente

- `PREPARACION_DATOS`: no existe `qa-owner-a@example.test` en Supabase local al 2026-09-08
- `ESTADO`: `NO_EJECUTADO`
- `APROBADOR`: `PENDIENTE`
